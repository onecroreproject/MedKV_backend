# Server Configuration
PORT=5000
NODE_ENV=production

# Database
MONGO_URI=mongodb://localhost:27017/medicalkv

# CORS - Comma separated list of allowed origins (e.g., https://medicalkv.com,https://admin.medicalkv.com)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Add any other required secrets below (JWT secrets, Email credentials, Razorpay keys, etc.)
# JWT_SECRET=your_jwt_secret_key
# JWT_EXPIRES_IN=7d
